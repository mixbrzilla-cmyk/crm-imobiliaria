export const revalidate = 0;
export const dynamic = "force-dynamic";

import EmpreendimentosClient from "./EmpreendimentosClient";

export default function EmpreendimentosPage() {
  return <EmpreendimentosClient />;
}
